module.exports = {
  plugins: {
    // Use the Tailwind PostCSS plugin package (newer Tailwind versions moved the PostCSS
    // integration into `@tailwindcss/postcss`). Install it with:
    //   npm install -D @tailwindcss/postcss
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
